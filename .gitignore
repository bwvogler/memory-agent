.env
.env.*
!.env.example
__pycache__/
*.py[cod]
.venv/
venv/
work/
mnt/
# scripts/mount-kb.sh --dev keeps its mountpoint and savepoints apart from the
# production ones, so it needs its own pair here.
work-dev/
*.log
.DS_Store
.vscode/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
*.gate.lock*
